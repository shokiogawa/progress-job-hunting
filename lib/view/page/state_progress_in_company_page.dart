import 'package:flutter/material.dart';

class StateProgressInCompanyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("選考状況"),
        centerTitle: true,
      ),
    );
  }
}
